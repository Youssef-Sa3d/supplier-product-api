{
  "devDependencies": {
    "@cap-js/cds-test": "^0.4.1",
    "@cap-js/cds-typer": ">=0.1",
    "@cap-js/cds-types": "^0.16.0",
    "@sap/cds-dk": "^9.8.1",
    "@types/jest": "^30.0.0",
    "@types/node": "^25.5.0",
    "jest": "^30.3.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5"
  },
  "dependencies": {
    "@cap-js/sqlite": "^2.2.0",
    "@sap/cds": "^9.8.1",
    "tsx": "^4"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "url": "db/supplier-product.db"
        }
      },
      "auth": {
        "kind": "basic",
        "users": {
          "admin": {
            "password": "admin123",
            "roles": [
              "admin"
            ]
          }
        }
      }
    },
    "[production]": {
      "requires": {
        "db": {
          "kind": "sqlite",
          "credentials": {
            "url": "/tmp/supplier-product.db"
          }
        }
      }
    },
    "typescript": true,
    "test": {
      "requires": {
        "db": {
          "kind": "sqlite",
          "credentials": {
            "url": ":memory:"
          }
        }
      }
    }
  },
  "scripts": {
    "start": "node node_modules/@sap/cds/bin/deploy.js && node node_modules/@sap/cds/bin/serve.js",
    "watch": "cds watch",
    "deploy": "cds deploy --to sqlite:db/supplier-product.db",
    "test": "jest --testEnvironment node",
    "build": "tsc --outDir gen/srv"
  },
  "imports": {
    "#cds-models/*": "./@cds-models/*/index.js"
  }
}